package da;

import java.util.List;

// Compile-only stub. Real da.b.d() returns the flat list of every e8.j node in the
// current window's tree (root included), already built by the app's own AccessibilityService walk.
public class b {
    public List d() { return null; }
}
