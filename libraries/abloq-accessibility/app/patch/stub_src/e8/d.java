package e8;

import android.graphics.RectF;

// Compile-only stub. Field `c` is the node's bounds already mapped into screen coordinates.
// (Ground truth per baksmali: `.field public final c:Landroid/graphics/RectF;`. jadx cosmetically
// renames this to `f9090c` for display — never trust that name; see RECON.md section 5a.)
public class d {
    public final RectF c = null;
}
