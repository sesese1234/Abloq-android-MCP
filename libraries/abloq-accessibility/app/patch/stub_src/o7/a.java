package o7;

// Compile-only stub matching the real com.appsisle.developerassistant internal API
// (o7.a is the app's static service locator, initialized in DevAssistApplication.onCreate()).
// Never shipped: only classes under com.abloq.bridge are dexed and merged into the patched APK.
public class a {
    public static a e() { return null; }
    public q7.d g() { return null; }
}
